{
  "name": "futu_node_api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/futu.js",
  "scripts": {
    "test": "npm run update; mocha dist/src/test/test.js",
    "prebuild": "chmod +x tools/*.sh; ./tools/pb2ts.sh; ./tools/update_proto_id.sh",
    "build": "npm run update",
    "update": "tsc; for f in $( ls src/proto ); do ln -s $PWD/src/proto/${f} $PWD/dist/src/proto/${f} >> /dev/null; done"
  },
  "author": "Petel__",
  "license": "ISC",
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.6.8",
    "axios": "^0.19.0",
    "cheerio": "^1.0.0-rc.3",
    "mocha": "^6.2.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "hex-sha1": "^1.0.2",
    "protobufjs": "^6.8.8"
  }
}
